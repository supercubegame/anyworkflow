name: verify

on:
  push:
    branches: ['**']
  workflow_dispatch:

jobs:
  backup-repo-gate:
    name: 备份仓闸门
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: 跑最小闸门
        shell: bash
        run: |
          set -euo pipefail
          python3 verify.py
